{



  "type": "module",
  "name": "test-react",
  "version": "0.1.0",
  "license": "MIT",
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "*",
    "@testing-library/user-event": "*",
    "react": "*",
    "react-dom": "*",
    "react-scripts": "*",
    "web-vitals": "*"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
      "one_test": "CI=true react-scripts test",
      "one-test": "CI=true react-scripts test",
      "test-all": "react-scripts test --changedSince d824cf5",

      "test-trans" : "react-scripts test -- src/trans.cjs ",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/react": "^15.0.7",
    "react-test-renderer": "*"
  }
}
